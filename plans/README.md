# Implementation Plans

Generated by the improve skill on 2026-06-13. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Establish a verification baseline | P1 | M | - | DONE |
| 002 | Constrain patch filesystem paths | P1 | S | 001 | TODO |
| 003 | Clear disconnected worklet inputs | P1 | M | 001 | TODO |
| 004 | Validate patch payloads before persistence/import | P1 | M | 001, 002 | TODO |
| 005 | Release external module resources on teardown | P1 | S | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale - finding fixed independently or approach abandoned)

## Dependency Notes

- 001 should land first because the repo currently has no meaningful automated test entrypoint. Later plans rely on its scripts and test layout.
- Reconciliation on 2026-06-14 found Plan 001 applied in the working tree but not committed, and not fully verified. Do not execute dependent plans until the app typecheck blocker is resolved and Plan 001 is marked DONE again.
- 004 should run after 002 because both edit the patch API route files. If one executor takes both plans, apply 002 first, then add validation on top.
- 003 and 005 are independent after 001 and can run in parallel with 002/004 if different executors coordinate file ownership.

## Verification Gates From Recon

| Area | Command | Expected on success |
|------|---------|---------------------|
| App lint | `cd app && bun eslint .` | exit 0, no lint errors |
| App build | `cd app && bun run build` | exit 0 |
| Audio processors build | `cd audio-processors && bun run build` | exit 0 |
| WASM-only build | `cd audio-processors/wasm && wasm-pack build --target=web --out-dir ../pkg` | exit 0 |

Plan 001 adds first-class test/typecheck scripts. After it lands, use the plan-specific commands in each file.

## Reconciliation Log

### 2026-06-14

- Current commit: `231e15b`.
- Working tree contains uncommitted Plan 001 changes plus untracked `plans/` files.
- Verified passing: `cd app && bun run test`, `cd app && bun run lint`, `cd audio-processors && bun run typecheck`, and `cd audio-processors && bun run test:rust`.
- Verification blocker: `cd app && bun run typecheck` exits 1 because `app/components/modules/MidiInputModule.client.vue:305` passes `midiStore.midiInputs` directly to `USelect` `items`, whose item `type` property conflicts with Nuxt UI `SelectItem.type`.
- Plans 002-005 findings were spot-checked and still exist in the live code. They remain TODO, but are not executable while Plan 001 is BLOCKED.
- No artifact-producing builds were run during reconciliation.

### 2026-06-14 (second reconciliation)

- Current commit: `231e15b`.
- Unblocked Plan 001 by fixing the typecheck blocker: added `midiInputOptions` computed in `MidiInputModule.client.vue` that maps `midiStore.midiInputs` to plain `{ id, name }` objects, avoiding the `MIDIPortType.type` vs `SelectItem.type` conflict.
- Full verification baseline passes: `cd app && bun run test` (4 tests), `cd app && bun run typecheck`, `cd app && bun run lint`, `cd audio-processors && bun run test` (4 Rust tests).
- Plan 001 marked DONE. Plans 002-005 are now unblocked and executable.

## Findings Considered And Rejected Or Deferred

- Real-time envelope DSP logging and sequencer message throttling were confirmed but deferred because the default plan set prioritizes security/correctness and the missing verification baseline first.
- Dirty-state hashing and per-module selection scans were confirmed but deferred as performance follow-ups after the higher-risk persistence and AudioWorklet correctness issues.
- Module metadata/render registry consolidation was confirmed but deferred because it is large, high-risk architecture work that should follow characterization tests and smaller correctness fixes.
- Stale setup docs and duplicate builder-path cleanup were confirmed but deferred because they are lower operational risk than patch persistence and audio resource cleanup.
